# Copy this to .env for local dev, or set in Netlify dashboard → Site settings → Environment variables

# Your Google Apps Script web app URL (never exposed to the browser)
GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
